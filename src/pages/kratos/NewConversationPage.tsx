import {
  ConversationWorkspace,
  type ConversationWorkspaceProps,
} from "@/widgets/kratos/conversation/ConversationWorkspace"

export function NewConversationPage(props: ConversationWorkspaceProps) {
  return <ConversationWorkspace {...props} />
}
